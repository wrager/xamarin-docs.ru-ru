---
title: "Устранение неполадок"
ms.topic: article
ms.prod: xamarin
ms.assetid: B50FE9BD-9E01-AE88-B178-10061E3986DA
ms.technology: xamarin-ios
author: bradumbaugh
ms.author: brumbaug
ms.date: 05/22/0201
ms.openlocfilehash: 95d4bfd78ee77f9afafce61f52b7874299df543d
ms.sourcegitcommit: 8e722d72c5d1384889f70adb26c5675544897b1f
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 03/15/2018
---
# <a name="troubleshooting"></a>Устранение неполадок

## <a name="xamarinios-cannot-resolve-systemvaluetuple"></a>Xamarin.iOS не удается разрешить System.ValueTuple

Эта ошибка возникает из-за несовместимости с Visual Studio.

- **Visual Studio 2017 г. обновление 1** (версия 15.1 или более ранних версий) совместим только с **System.ValueTuple NuGet 4.3.0** (или более ранних версий).

- **Visual Studio 2017 г. обновление 2** (15.2 или более поздняя версия) совместим только с **System.ValueTuple NuGet 4.3.1** или более поздней версии.

Выберите правильный System.ValueTuple NuGet, соответствующий установленной системы Visual Studio 2017 г.


## <a name="receiving-error-retrieving-update-information-error-message"></a>Получает сообщение «Ошибка при получении сведений об обновлениях»

При попытке обновления программного обеспечения и это сообщение об ошибке отображается, попробуйте перезапустить вашей интегрированной среды разработки и выход, а затем обратно в свою учетную запись в Интегрированной среде разработки.

## <a name="how-do-i-create-outlets-or-actions-with-interface-builder"></a>Создание выходов или действия с помощью построителя интерфейс?

С появлением конструктор Xamarin IOS в Visual Studio для Mac и Visual studio разработчики Xamarin.iOS теперь могут воспользоваться преимуществами Создание пользовательского интерфейса через раскадровки и .xibs. Ссылаться на [Привет, iOS](~/ios/get-started/hello-ios/index.md) направляющие Дополнительные сведения об использовании конструктора.

Можно также ссылаться на Apple [розетки](https://developer.apple.com/library/ios/recipes/xcode_help-IB_connections/chapters/CreatingOutlet.html) и [действия](https://developer.apple.com/library/ios/recipes/xcode_help-IB_connections/chapters/CreatingAction.html) направляющие Дополнительные сведения об использовании в IB выходов и действия.

## <a name="systemtextencodinggetencoding-throws-notsupportedexception"></a>System.Text.Encoding.GetEncoding создает исключение NotSupportedException

Возможно, используется с кодировкой, которая не добавляется по умолчанию. Проверьте [интернационализации](~/ios/app-fundamentals/localization/index.md) страницу, чтобы узнать, как добавить поддержку дополнительных кодировки.

## <a name="systemmissingmethodexception-anything-else"></a>System.MissingMethodException (нибудь)

Элемент удален компоновщиком скорее всего и таким образом, не существует в сборке во время выполнения.  Существует несколько решений:

-  Добавить [[Сохранить]](http://www.go-mono.com/docs/index.aspx?link=T:MonoTouch.Foundation.PreserveAttribute) для элемента атрибута.  Это будет препятствовать компоновщик его удаления.
-  При вызове [mtouch](http://www.go-mono.com/docs/index.aspx?link=man:mtouch%281%29) , используйте **- nolink** или **- linksdkonly** параметры. -    **-nolink** отключает все связывания.
-    **-linksdkonly** Xamarin.iOS техники сборки, связать например *monotouch.dll* или xamarin.ios.dll.

Обратите внимание на связанные сборки, чтобы меньше полученный исполняемый файл; Таким образом отключение связывания может привести к большой исполняемый файл не является предпочтительным.

## <a name="you-are-getting-a-modelnotimplementedexception"></a>Вы получаете ModelNotImplementedException

Если вы получаете это исключение, это означает, вы вызываете базового. Метод () на класс, переопределяющий модели. Необходимо вызвать базовый метод в классе для моделей (это классы, помеченные атрибутом [модель]).

## <a name="this-class-is-not-key-value-coding-compliant-for-the-key-xxxx"></a>Этот класс не является значение ключа кодирования совместимое ключа XXXX

При появлении этой ошибки при загрузке файла NIB означает, что значение XXXX не найден управляемого класса. Это означает отсутствие объявления следующим образом:

```csharp
[Connect]
TypeName XXXX {
   get {
       return (TypeName) GetNativeField ("XXXX");
   }
   set {
       SetNativeField ("XXXX", value);
   }
}
```

Выше определения автоматически создается средой Visual Studio для Mac для всех файлов XIB, добавлять в Visual Studio для Mac в `NAME_OF_YOUR_XIB_FILE.designer.xib.cs` файла.

Кроме того, типов, содержащих этот код должен быть подклассом [NSObject](https://developer.xamarin.com/api/type/Foundation.NSObject/).  Если содержащий тип в пространстве имен, должна быть [[регистрация]](https://developer.xamarin.com/api/type/Foundation.RegisterAttribute/) атрибут, который предоставляет имя типа без пространства имен (как построитель интерфейс не поддерживает пространства имен в типах):

```csharp
namespace Samples.GLPaint {

    // The [Register] attribute overrides the type name registered
    // with the Objective-C runtime, in this case removing the namespace.
    [Register ("AppDelegate")]
    public class AppDelegate {/* ... */}
}
```

## <a name="unknown-class-xxxx-in-interface-builder-file"></a>Неизвестный класс XXXX в файле интерфейс построителя

Эта ошибка возникает в том случае, если определить класс в файлах построителя интерфейса, но не предоставить фактическую реализацию для него в коде C#.

Необходимо добавить некоторый код следующим образом:

```csharp
public partial class MyImageView : UIView {
   public MyImageView (IntPtr handle) : base (handle {}
}
```
## <a name="systemmissingmethodexception-no-constructor-found-for-foobarctorsystemintptr"></a>System.MissingMethodException: Не удалось найти конструктор для Foo.Bar::ctor(System.IntPtr)

Эта ошибка возникает во время выполнения, когда код пытается создать экземпляр классы, на который существует ссылка из файла интерфейс построителя. Это означает, что вы не добавляли конструктор, принимающий один IntPtr в качестве параметра.

Конструктор с дескриптором IntPtr используется для привязки объектов, управляемых с помощью представлений в неуправляемом коде.

Чтобы устранить эту проблему, добавьте следующий код в класс Foo.Bar:

```csharp
public Bar (IntPtr handle) : base (handle) { }
```
## <a name="type-foo--does-not-contain-a-definition-for-getnativefield-and-no-extension-method-getnativefield-of-type-foo-could-be-found"></a>Тип {Foo} не содержит определение для `GetNativeField' and no extension method `GetNativeField "типа может быть найден {Foo}

При появлении этой ошибки в конструкторе созданные файлы (*. xib.designer.cs), это означает, что одно из следующих действий:

 **(1) отсутствует разделяемого класса или базового класса**

Разделяемые классы, автоматически созданный конструктором должен иметь соответствующий разделяемых классов в пользовательском коде, которые наследуют некоторые подкласс `NSObject`, часто `UIViewController`. Убедитесь, что имеется такой класс для типа, который дает ошибку.

 **(2) пространства имен по умолчанию изменено**

Файлы конструктора создаются с помощью параметров пространства имен по умолчанию проекта. Если после изменения этих параметров, или переименовать проект, созданный разделяемые классы больше не может быть в том же пространстве имен, совпадают с аналогичными пользовательского кода.

Параметры пространства имен можно найти в диалоговом окне Параметры проекта. Пространство имен по умолчанию находится в **Общие -> Параметры Main** раздела. Если поле пусто, имя проекта используется по умолчанию. Дополнительные параметры пространства имен можно найти в **исходный код -> политики именования .NET** раздела.

## <a name="warning-for-actions-the-private-method-foo-is-never-used-cs0169"></a>Предупреждение для действий: «Foo» никогда не используется закрытый метод. (CS0169)

Действия для интерфейса построитель файлы подключены к мини-приложений путем отражения во время выполнения, поэтому ожидается, это предупреждение.

Можно использовать «#pragma warning отключить 0169» «включить #pragma warning 0169" вокруг свои действия, если вы хотите отключить это предупреждение только для этих методов, или добавьте 0169 полю «Пропускать предупреждения» в параметры компилятора, если вы хотите отключить ее для всего проекта (не рекомендуется).

## <a name="mtouch-failed-with-the-following-message-cannot-open-assembly-pathtoyourprojectexe"></a>не удалось выполнить mtouch со следующим сообщением: не удается открыть сборку "/ path/to/yourproject.exe"

Если вы видите это сообщение об ошибке, обычно проблема заключается в том абсолютный путь проекта содержит пробел. Эта проблема будет устранена в будущих версиях Xamarin.iOS, но можно обойти эту проблему, переместив проекта в папке без пробелов.

## <a name="your-sqlite3-version-is-old---please-upgrade-to-at-least-v350"></a>Старые версии sqlite3 - обновите по крайней мере v3.5.0!

Это происходит при выполнении всех следующих:

1.  Использовать Mono.Data.Sqlite
1.  Использовать Mac OS X Leopard (10.5)
1.  Запуск приложения в симуляторе.


Проблема заключается в том, что моно комплектует OS X `libsqlite3.dylib`, не iPhoneSimulator в `libsqlite3.dylib` файла. Приложение *будет* работают на устройство, но не только к симулятора.

## <a name="deploy-to-device-fails-with-systemexception-amdeviceinstallapplication-returned-3892346901"></a>Развертывание на устройстве завершается System.Exception: AMDeviceInstallApplication возвращается 3892346901

Эта ошибка означает, что конфигурация подписи кода для свой идентификатор сертификата и пакета соответствует профиля подготовки, установленные на вашем устройстве.  Убедитесь, у вас есть соответствующий сертификат выбранный в параметрах проекта -> iPhone подписывание пакета и идентификатор правильный пакет, указанный в параметрах проекта -> приложения iPhone

## <a name="code-completion-is-not-working-in-visual-studio-for-mac"></a>Дополнение кода не работает в Visual Studio для Mac

Убедитесь, что вы используете последнюю версию Visual Studio для Mac и Xamarin.iOS

Если проблема остается, обратитесь за помощью [файл ошибку](http://monodevelop.com/Developers#Reporting_Bugs), присоединение **~/Library/Logs/XamarinStudio-{VERSION}/Ide-{TIMESTAMP}.log**, **AndroidTools-{TIMESTAMP} .log**, и **компонентов-{TIMESTAMP} .log** файлы журнала.

Если ничего не поможет, можно попробовать удалить кэш кода завершения, чтобы его повторном создании:

 `[rm -r ~/.config/XamarinStudio-{VERSION}/CodeCompletionData]`

Будьте внимательны, введите следующую команду, правильно, или можно случайно удалить важных файлов.

## <a name="visual-studio-for-mac-crashes-when-you-copy-text"></a>Visual Studio для Mac аварийно завершает работу при копировании текста

Популярные программы Mac QuickSilver, Google инструментов и панели запуска имеют функции буфера обмена, повреждены Visual Studio для Mac в памяти. В их параметры можно составить список Visual Studio для Mac в процессе, они не должна взаимодействовать с.

## <a name="visual-studio-for-mac-complains-about-mono-24-required"></a>Visual Studio для Mac сообщает о необходимых 2.4 моно

Если обновления Visual Studio для Mac из-за последние обновления, а при попытке запустить его снова его ошибка сообщает об моно 2.4, не существует, необходимо сделать достаточно [обновление установки 2.4 моно](http://www.go-mono.com/mono-downloads/download.html).  

Моно 2.4.2.3_6 устраняет некоторые важные проблемы, которые не Visual Studio для Mac запускаются надежно и иногда зависших Visual Studio для Mac во время запуска или запрещено создается база данных для выполнения кода.

После установки нового моно Visual Studio для Mac будет запускаться должным образом.

## <a name="assertion-at-monometadatageneric-sharingc704-condition-oti-not-met"></a>Утверждение по... /.. /.. /.. /Mono/Metadata/Generic-Sharing.c:704 oti не выполнены условия

Если вы получили следующей трассировкой стека:

```csharp
 - Assertion at ../../../../mono/metadata/generic-sharing.c:704, condition `oti' not met
Stacktrace:
    at System.Collections.Generic.List`1<object>..cctor () <0xffffffff>
    at System.Collections.Generic.List`1<object>..cctor () <0x0001c>
    at (wrapper runtime-invoke) object.runtime_invoke_dynamic (intptr,intptr,intptr,intptr) <0xffffffff>`
```

Это означает, что связывание производится откомпилировано thumb кода в проект статической библиотеки. Начиная с выпуска пакета SDK для iPhone 3.1 (или более поздней версии на момент написания этой статьи) Apple реализовала ошибки в своих компоновщика при связывании кода не Thumb (Xamarin.iOS) с кодом Thumb (статической библиотеки). Необходимо связать с версией статической библиотеки для устранения этой проблемы не Thumb.

## <a name="systemexecutionengineexception-attempting-to-jit-compile-method-wrapper-managed-to-managed-foosystemcollectionsgenericicollection1getcount-"></a>System.ExecutionEngineException: Попытка (JIT компиляции (управляемый в управляемые оболочки) метод Foo[]:System.Collections.Generic.ICollection'1.get_Count)

Суффикс [] Указывает, что вы или библиотеки классов вызов метода массив через универсальной коллекции, например IEnumerable <>, ICollection <> или IList <>. Чтобы избежать этого можно явным образом данном force AOT компилятору включить такой метод путем вызова метода самостоятельно и, что этот код выполняется до вызова метода, вызвавшей исключение. В этом случае можно написать:

```csharp
Foo [] array = null;
int count = ((ICollection<Foo>) array).Count;
```

Который будет принудительно AOT компилятору включить метод get_Count.

## <a name="visual-studio-for-mac-source-editor-is-extremely-slow"></a>Visual Studio для редактора источника Mac резко снизит скорость обработки

Иногда Visual Studio для Mac Редактор источника становится слишком медленно, кажется зависания несколько секунд между ввода символов.

Эта проблема не будет очень редко и очень сложно воспроизвести — он обычно не могут быть воспроизведены на том же компьютере после перезапуска Visual Studio для Mac. По этой причине мы будем признательны, если можно выполнить ряд действий по отладке до перезапуска Visual Studio для Mac и отправить нам результаты.

1.  Попробуйте закрыть вкладка редактора и повторного открытия. Занимает немного редактирования или перемещения курсора до замедление появится снова?
1.  Отключить «Луч Sync» с помощью средства разработчика «Quartz Debug» (который можно найти с помощью Spotlight) и проверьте ли производительность редактора источника восстанавливается значение normal.
1.  Повторите шаг (1) с синхронизацией луч по-прежнему отключен.
1.  Если редактор зависает на несколько секунд, попробуйте выполнить «KillAll значение-QUIT [Visual Studio для Mac]» в конечном во время его завис. Часто бывает трудно время команды kill происходит зависание редактор, но важно сделать это, поскольку команда заставляет Mono для записи трассировок стека всех потоков журнал MD, который можно использовать для обнаружения состояния потоки находятся в пока зависание XS.



Подключите журналы XS **~/Library/Logs/XamarinStudio-{VERSION}/Ide-{TIMESTAMP}.log**, **AndroidTools-{TIMESTAMP} .log**, и **компонентов-{TIMESTAMP} .log**(в предыдущих версиях XS/MonoDevelop, просто отправьте **~/Library/Logs/MonoDevelop-(3.0|2.8|2.6)/MonoDevelop.log**).

 **Примечание: Выше проблема была исправлена в окончательный XS 2.2**

## <a name="compiled-application-is-very-large"></a>Скомпилированное приложение слишком велик

Для поддержки отладки, в отладочных построениях содержат дополнительный код. Проекты, построенные в режиме выпуска — это доля размера.

Начиная с версии 1.3 Xamarin.iOS отладочные сборки включены отладки поддержка каждый один компонент моно (каждый метод в каждом классе платформы).  

С помощью Xamarin.iOS 1.4 мы представим более настроенную метод для отладки, по умолчанию будет использоваться только обеспечивают отладки инструментирования для кода и библиотек, а этого не для всех [моносборок](~/cross-platform/internals/available-assemblies.md) (это будет по-прежнему можно, но необходимо будет явно согласиться на отладку этих сборок).

## <a name="installation-hangs"></a>Установка зависает

Моно и Xamarin.iOS установщики зависания при наличии работу симулятора iPhone. Эта проблема не ограничивается моно или Xamarin.iOS, это постоянная проблема через программное обеспечение, которое пытается установить программное обеспечение на MacOS Snow Leopard, если iPhone симулятор выполняется во время установки.

Убедитесь в том, закройте имитатор iPhone и повторить попытку установки.

<a name="trampolines" />

## <a name="ran-out-of-trampolines-of-type-0"></a>Вышел за пределы trampolines типа 0

Если получить это сообщение во время выполнения устройства, можно создать дополнительные тип 0 trampolines (ОПРЕДЕЛЕННОГО типа), изменив параметры вашего проекта, раздел «iPhone построения».  Вы хотите добавить лишние аргументы для устройства построения целевых объектов:

 `-aot "ntrampolines=2048"`

Количество trampolines по умолчанию — 1024.  Попробуйте увеличить это число, пока не будет достаточно для вашего приложения.

## <a name="ran-out-of-trampolines-of-type-1"></a>Вышел за пределы trampolines типа 1

При необходимости использовании универсальных шаблонов рекурсивные может получить это сообщение на устройстве.  Можно создать дополнительные типа 1 trampolines (тип RGCTX), изменив параметры вашего проекта, раздел «iPhone построения».  Вы хотите добавить лишние аргументы для устройства построения целевых объектов:

 `-aot "nrgctx-trampolines=2048"`

Количество trampolines по умолчанию — 1024.  Попробуйте увеличить это число, пока не будет достаточно для использования универсальных шаблонов.

## <a name="ran-out-of-trampolines-of-type-2"></a>Вышел за пределы trampolines типа 2

При внесении интенсивного использования интерфейсов, может получить это сообщение на устройстве.
Можно создать дополнительные типа 2 trampolines (тип преобразователи IMT), изменив параметры вашего проекта, раздел «iPhone построения».  Вы хотите добавить лишние аргументы для устройства построения целевых объектов:

 `-aot "nimt-trampolines=512"`

Число trampolines IMT преобразователь по умолчанию — 128.  Попробуйте увеличить это число, пока не будет достаточно для использования интерфейсов.

## <a name="debugger-is-unable-to-connect-with-the-device"></a>Отладчику не удается подключиться к устройству

При запуске отладки конфигурации устройства, вы увидите, что отладчик Показать диалоговое окно, указывающее, что он пытается подключиться к приложению. Существует несколько причин, по которым отладчик не сможет подключиться к приложению, в зависимости от режима, используется для подключения (USB или Wi-Fi).

 **Если устройство и узел отладчика, находятся в разных сетях**, брандмауэр или частную сеть может препятствовать приложения из подключения к узлу отладчик в режиме Wi-Fi.

 **Visual Studio для Mac не могут иметь возможность направлять запрос правильный IP-адрес узла**. В Wi-Fi режим Visual Studio для Mac предоставляет приложению все IP-адреса, он может найти узла и приложение попытается их все для просмотра, если она может использовать любой из них для подключения к Visual Studio для Mac.

 **Другое устройство подключено к USB-порту на узле.** В некоторых случаях с другими устройствами, подключенными к USB-ПОРТУ портов на узле известно, к каким-либо образом повлиять на отладку в режиме USB.

Если режим Wi-Fi или USB не работает, можно легко попробовать другой: Откройте в Visual Studio для Mac параметров, перейдите на страницу отладчик отладчик/параметры/iPhone и переключать флажок «Debug устройства iOS по Wi-Fi, а не по USB».   Если не работает, вы увидите Дополнительные сведения об ошибке в консоль устройства в подробном режиме (которой включается путем добавления «-v - v - v» на mtouch дополнительные аргументы в параметрах проекта).

## <a name="error-134-mtouch-failed-with-the-following-message"></a>: 134 mtouch ошибка со следующим сообщением:

Эта ошибка может возникать, если выполняется построение с - nolink стиль Xamarin.iOS 1.4 выпусков. Можно обойти эту ошибку путем указания дополнительных аргументов в monodevelop конфигурации проекта.

Добавьте аргумент

 `-nosymbolstrip`

и должны быть исправлены проблемы.

## <a name="distribution-identity-is-not-shown-in-visual-studio-for-mac-project-signing-options"></a>Удостоверение для распространения не отображается в Visual Studio для Mac проекта параметры создания подписей

Visual Studio для Mac 2.2 обнаруживается ошибка, вызвавшим обнаружение распространения сертификатов, содержащих запятую. Выполните обновление до Visual Studio для Mac 2.2.1.

## <a name="error-afcfilerefwrite-returned-1-during-upload"></a>Ошибка «возвращается AFCFileRefWrite: 1» во время загрузки

При отправке приложения на устройство может появиться ошибка «возвращается AFCFileRefWrite: 1». Это может произойти, если имеется файл нулевой длины.

## <a name="error-mtouch-failed-with-no-output"></a>Ошибка «mtouch завершилась без выходных данных»

Текущий выпуск Xamarin.iOS и Visual Studio для Mac fail, если имя проекта или каталог, в котором хранятся решение или проект, содержащие пробелы.
Чтобы устранить эту проблему:


-  Убедитесь, что ни в вашем проекте, либо в каталоге, где она хранится содержит пробел.
-  Убедитесь, что имя проекта не содержит пробелов в параметры проекта, «Main».

## <a name="error-the-binary-you-uploaded-was-invalid-a-pre-release-beta-version-of-the-sdk-was-used-to-build-the-application"></a>Ошибка «двоичный файл, который вы отправили недопустимый. В предварительной версии бета-версии пакета SDK использовался для построения приложения»

Эта ошибка обычно происходит с проектом, который был запущен в iPad разработки до выпуска Xamarin.iOS 2.0.0, скорее всего имеет некоторые ключей в вашей Info.plist как:

```xml
<key>UIDeviceFamily</key>
       <array>
               <string>1</string>
       </array>
```

Этой пары ключей должны быть удалены, так как Visual Studio для Mac обрабатывает его автоматически.

## <a name="error-a-pre-release-beta-version-of-the-sdk-was-used-to-build-the-app"></a>Ошибка «предварительная бета-версия пакета SDK использовался для построения приложения»

(Представленные Ed Anuff)

Выполните следующие действия.

-  Изменение подключения версию пакета SDK в iPhone сборки для версии 3.2 или iTunes отклонит его передачи, так как он видит совместимые приложения iPad, созданного с помощью пакета SDK версии меньше, чем 3.2
-  Создание пользовательских Info.plist для проекта и явно задать MinimumOSVersion 3.0 в нем.   Значение MinimumOSVersion 3.2 Xamarin.iOS будут переопределены.   Если этого не сделать, приложение не будет работать на iPhone.
-  Подключите перестроения, zip и отправить в iTunes.

## <a name="unhandled-exception-systemexception-failed-to-find-selector-someselector-on-type"></a>Необработанное исключение: System.Exception: не удалось найти селектор someSelector: на {type}

Это исключение возникает одно из трех действий:


1.  Вы указали селектор для среды выполнения C цель без применения к методу соответствующего атрибута [экспорта]
1.  Разрешено полная компоновка и не применяется атрибут [Сохранение] [экспорта] ed метода.
1.  Атрибут [экспорта] был применен к закрытому методу унаследованного типа.


## <a name="mainwindowxibdesignercs-file-is-not-updated"></a>Файл MainWindow.xib.designer.cs не обновляется

Произошла ошибка в Xamarin Studio 2.4, вызванная не группировать файлы MainWindow.xib и MainWindow.xib.designer в новых проектах. Это означает, что не обновит кода конструктора для конкретного файла.

Эта проблема исправлена в версии Visual Studio для Mac, который доступен в ее встроенного средства обновления, убедитесь в использовании новой версии.

Существующие проекты можно устранить, удалив (но не удаления) xib и его файл конструктора, затем добавление его обратно. Это следует повторно группировать файлы правильно.

## <a name="uialertview-or-uiactionsheet-vanish-after-being-created"></a>UIAlertView или UIActionSheet удалить после создания

Если у вас есть некоторый код следующим образом:

```csharp
var actionSheet = new UIActionSheet ("My ActionSheet", null, null, "OK", null){
   Style = UIActionSheetStyle.Default
};

actionSheet.Clicked += delegate (sender, args){
    Console.WriteLine ("Clicked on item {0}", args.ButtonIndex);
};
```

как временной переменной в функции находятся объекты «actionSheet» и сразу после завершении функции, объект подлежит для сборки мусора, поэтому он в конечном итоге сборщиком мусора.

Чтобы устранить эту проблему, необходимо сохранить ссылку на «actionSheet» за пределами метода, где-нибудь, будет располагаться за пределами метода.

## <a name="project-always-runs-in-the-ipad-simulator"></a>Проект запусков всегда в симуляторе iPad

Установщик iPhone SDK 4.0 устанавливает 2 SDK - 3,2 пакета SDK для создания приложений, доступных только для iPad и 4.0 SDK, используемый для iPhone надписи и универсальных приложений. Он также устанавливает 3,2 симулятор, который имитирует только iPad, а 4.0 симулятор, которое имитирует iPhone или iPhone 4. Удаляются все более старые пакеты SDK и имитаторы.

Visual Studio для параметров построения проекта iPhone Mac входят параметра для версии пакета SDK, который будет использоваться для создания приложения. Этот параметр можно найти в **параметры проекта "->" сборки "->" iPhone построения**.

Новые проекты в Visual Studio для Mac использовать старые установленным пакетом SDK в качестве их значения по умолчанию пакета SDK и Visual Studio для Mac, если указанный пакет SDK не существует, будет использовать ближайший он может найти для создания приложений. Это было сделано, чтобы проекты не всегда будет requre новейшие SDK. Однако в настоящее время результатом 3,2 SDK, используется - в результате используется имитатор iPad.

Чтобы устранить эту проблему с помощью пакета SDK версии 4.0, перейдите к **параметры проекта "->" сборки -> iPhone построения**> и измените значение SDK на использование в раскрывающемся списке «4.0». Это необходимо сделать для каждого сочетание конфигурации и платформы, получить доступ с помощью раскрывающихся списков в верхней части панели.

Не следует путать с параметром «Версия ОС минимум» версию пакета SDK.
Это значение не обязательно совпадают с значение версии пакета SDK — это изменение отразится на минимальную версию операционной системы будет установить приложения, который может быть более ранней, чем пакет SDK, а используйте только те API, которые существуют в старой операционной системе или предотвратить использование более новыми функциями, проверка версии операционной системы среды выполнения с помощью Ks. Следует задать значение самую старую версию ОС, на котором вы тестируете приложение.

Обратите внимание, что **проекта -> iPhone цель в симуляторе**> меню можно использовать для выбора симулятора, используемый по умолчанию при выполнения или отладки проекта. Кроме того **выполнения -> запустить с**> меню можно использовать для выбора конкретных симулятор, для которой требуется выполнить

## <a name="ibtool-returns-error-133"></a>Возвращает ошибку 133, ibtool

Это означает, что имеется 4 XCode установлен.   В XCode 4 ibtool средство была удалена, ее можно изменить с помощью автономного инструмента XIB файлов.

Если вы хотите использовать интерфейс построителя, установить [XCode серии 3](http://connect.apple.com/cgi-bin/WebObjects/MemberSite.woa/wa/getSoftware?bundleID=20792), которые доступны из веб-сайт компании Apple.

## <a name="cant-create-display-binding-for-mime-type-applicationvndapple-wbrinterface-builder"></a>«Не удается создать привязку отображения для типа mime: application/vnd.apple -<wbr/>интерфейс построителя»

Эта ошибка возникает при попытке создать iPhone пользовательского интерфейса в проекте не iPhone. Убедитесь, что запуск с решением iPhone и iPad, невозможно просто прибавить iPhone элементы пользовательского интерфейса в проект не iPhone и iPad.

## <a name="startup-crash-when-executing-inside-the-ios-simulator"></a>Сбой запуска при выполнении в симуляторе iOS

Если сбой среды выполнения (SIGSEGV) появляется внутри имитатор вместе с трассировкой стека, который выглядит следующим образом:

```csharp
  at (wrapper managed-to-native) System.Reflection.Assembly.GetTypes (System.Reflection.Assembly,bool)
  at MonoTouch.ObjCRuntime.Runtime.RegisterAssembly (System.Reflection.Assembly)
  at (wrapper runtime-invoke) <Module>.runtime_invoke_void_object (object,intptr,intptr,intptr)
```
... Затем, возможно, имеется один (или более) устаревших сборка в каталоге приложения симулятора. Такие сборки может существует, поскольку Apple iOS simulator Добавление и обновление файлов, но никогда не удаляет их. Если это происходит, простейшим решением является симулятор меню выберите «Восстановить и содержимого и параметры...».   

**Предупреждение:** это приведет к удалению всех файлов, приложений и данных в симуляторе.   Следующем выполнении приложения, Visual Studio для Mac он будет развернут в симуляторе и будут существовать сборка не старого, устаревших вызвать сбой.

## <a name="simulator-hangs-during-application-installation"></a>Имитатор зависает при установке приложения

Это может произойти, если имена приложений "." (точка) в их именах.
Это запрещено как имя исполняемого файла в CFBundleExecutable -, даже если это возможно, работает во многих случаях (например, устройства).

 * "Значение не должно включать любое расширение по имени.» - [http://developer.apple.com/library/mac/documentation/General/Reference/InfoPlistKeyReference/InfoPlistKeyReference.pdf](http://developer.apple.com/library/mac/documentation/General/Reference/InfoPlistKeyReference/InfoPlistKeyReference.pdf)

## <a name="error-custom-attribute-type-0x43-is-not-supported-when-double-clicking-xib-files"></a>Ошибка: «настраиваемый атрибут 0x43 неподдерживаемый тип» при double, щелкнув .xib файлы

Это вызвано попытка открыть файлы .xib, если переменные среды заданы неправильно. Это не должно происходить с обычного использования Visual Studio для Mac/Xamarin.iOS и повторного открытия Visual Studio для Mac из /Applications должно устранить эту неполадку.

При попытке обновления программного обеспечения и это сообщение об ошибке отображается, обратитесь по электронной почте *support@xamarin.com*

## <a name="application-runs-on-simulator-but-fails-on-device"></a>Приложение выполняется в имитаторе, но не выполняется на устройстве

Эта проблема может проявляться в несколько форм и не всегда позволяет создавать согласованные ошибки. Если приложение содержит .xib, необходимо проверить правильность **действие при построении** на .xib задается **InterfaceDefinition**. Это действие построения по умолчанию для .xibs.

Чтобы проверить действие построения, щелкните правой кнопкой мыши файл .xib и выберите **действие при построении**.


## <a name="systemnotsupportedexception-no-data-is-available-for-encoding-437"></a>System.NotSupportedException: Данные недоступны для кодирования 437

При включении библиотеки сторонних производителей в приложении Xamarin.iOS, может появиться ошибка в форме «System.NotSupportedException: данные недоступны для кодирования 437» при попытке скомпилировать и запустить приложение. Например, библиотеки, такие как `Ionic.Zip.ZipFile`, может вызвать это исключение во время операции.

Это могут быть устранены, открыв параметры проекта Xamarin.iOS, перейдите в **сборка iOS** > **интернационализации** и проверка **Западная** Интернационализация.



<a name="Can't_upgrade_to_Indie/Business_from_Trial_Account" />


## <a name="cant-upgrade-to-indiebusiness-from-trial-account"></a>Невозможно обновить до Indie или Business из учетной записи пробной версии

Если приобретен недавно Xamarin.iOS и ранее запущен пробный Xamarin.iOS, может потребоваться выполнить следующие действия, чтобы получить это изменение лицензии выбираться средствами Visual Studio для Mac или Visual Studio.

-  Закройте Visual Studio для Mac или Visual Studio
-  Удалите все файлы из ~/Library/MonoTouch на Mac или %PROGRAMDATA%\MonoTouch\License\ для Windows
-  Снова откройте Visual Studio для Mac или Visual Studio и постройте проект Xamarin.iOS


## <a name="receiving-activation-incomplete-error-message"></a>Получает сообщение об ошибке «Активация неполные»

Эта проблема может возникать при использовании Xamarin.iOS для Visual Studio. Чтобы устранить эту проблему, отправьте журналы из следующего расположения для [ contact@xamarin.com ](mailto:contact@xamarin.com).

-  Расположение журнала: %LocalAppData%/Xamarin/Logs
