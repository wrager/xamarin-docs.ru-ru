---
title: Журнал выпуска цели Sharpie
description: В этом документе описывается журнал выпуска Sharpie цель, этот инструмент используется для автоматизации создания привязки C# для кода Objective-C.
ms.prod: xamarin
ms.assetid: 1F4A1BE1-7205-43F4-89D0-6C8672F52598
author: asb3993
ms.author: amburns
ms.date: 10/11/2017
ms.openlocfilehash: a173df769068a3834dfdc314675254af1f3594ed
ms.sourcegitcommit: ea1dc12a3c2d7322f234997daacbfdb6ad542507
ms.translationtype: MT
ms.contentlocale: ru-RU
ms.lasthandoff: 06/05/2018
ms.locfileid: "34781735"
---
# <a name="objective-sharpie-release-history"></a>Журнал выпуска цели Sharpie

## <a name="34-october-11-2017"></a>3.4 (11 октября 2017 г.)

[Загрузить v3.4.0](https://dl.xamarin.com/objective-sharpie/ObjectiveSharpie-3.4.0.pkg)

* Поддержка Xcode 9: 11, macOS iOS 10.13 tvOS 11 и watchOS 4
* Теперь должны быть исправлены проблемы с SIMD и tgmath
* Полностью удалить телеметрии

## <a name="33-august-3-2016"></a>3.3 (3 августа 2016 г.)

[Загрузить v3.3.0](https://download.xamarin.com/objective-sharpie/ObjectiveSharpie-3.3.0.pkg)

* Поддержка бета-версии 4 Xcode 8, iOS 10, macOS 10.12, tvOS 10 и watchOS 3.
* Обновлено до последней Clang главной сборки (2016-08-02)
* [Сохранение параметров отправки телеметрии](https://twitter.com/Symbiatch/status/760373403878559744) из `sharpie pod bind` для `sharpie bind`.

## <a name="32-june-14-2016"></a>3.2 (14 июня 2016 г.)

[Загрузить v3.2.3](https://download.xamarin.com/objective-sharpie/ObjectiveSharpie-3.2.3.pkg)

* Поддержка Xcode 8 бета-версии 1, iOS 10, macOS 10.12, tvOS 10 и watchOS 3.

## <a name="31-may-31-2016"></a>3.1 (31 мая 2016 г.)

[Загрузить v3.1.1](https://download.xamarin.com/objective-sharpie/ObjectiveSharpie-3.1.1.pkg)

* Поддержка CocoaPods 1.0
* Повышение надежности привязки CocoaPods построив собственный `.framework` и привязка, которая
* Скопируйте CocoaPods `.framework` и привязки в определение `Binding` directory упрощает интеграцию с проектами привязки Xamarin.iOS и Xamarin.Mac

## <a name="30-october-5-2015"></a>3.0 (5 октября 2015 г.)

[Загрузить v3.0.8](https://download.xamarin.com/objective-sharpie/ObjectiveSharpie-3.0.8.pkg)

* Поддержка для новых компонентов языка Objective-C, включая упрощенных универсальные типы и допустимость значений NULL, представленном в Xcode 7
* Поддержка последних iOS и Mac SDK.
* Создание и анализ поддержки Xcode проект! Теперь можно передать полного проекта Xcode Sharpie цель, и он будет выполнять его выполнение для выяснения правильно делать (например `sharpie bind Project.xcodeproj -sdk ios`).
* [CocoaPods](https://cocoapods.org) поддержки! Теперь настройка, построение и привязать CocoaPods непосредственно из Sharpie цель (например `sharpie pod init ios AFNetworking && sharpie pod bind`).
* При привязке платформ, Clang модули будут включены, если платформа поддерживает их, что приводит к наиболее подходящей синтаксический анализ является структурой, поскольку структуры framework определяется его `module.modulemap`.
* Для проектов Xcode, созданию framework продукта анализировать продукта вместо продукта промежуточных целевых объектов, как несистемных целевых объектов в проекте Xcode не устраняет неоднозначность, которые не могут быть автоматически разрешено.

## <a name="216-march-17-2015"></a>2.1.6 (17 марта 2015 г.)

[Загрузить v2.1.6](https://download.xamarin.com/objective-sharpie/ObjectiveSharpie-2.1.6.pkg)

* Фиксированный бинарного оператора выражения привязки: левая часть выражения был ошибочно заменены правой (например `1 << 0` неправильно были привязаны как `0 << 1`). Спасибо, что для Adam Kemp отметить это!
* Устранена проблема с `NSInteger` и `NSUInteger` привязываемый как `int` и `uint` вместо `nint` и `nuint` на i386; `-DNS_BUILD_32_LIKE_64` теперь передается Clang вносить синтаксический анализ `objc/NSObjCRuntime.h` работать должным образом на i386.
* Архитектура по умолчанию для пакетов SDK, Mac OS X (например `-sdk macosx10.10`) теперь является x86_64 вместо i386, поэтому `-arch` можно опустить, если не требуется значение по умолчанию.

## <a name="210-march-15-2015"></a>2.1.0 (15 марта 2015 г.)

[Загрузить v2.1.0](https://download.xamarin.com/objective-sharpie/ObjectiveSharpie-2.1.0.pkg)

* [bxc #27849](https://bugzilla.xamarin.com/show_bug.cgi?id=27849): Убедитесь, `using ObjCRuntime;` формируется, когда `ArgumentSemantic` используется.
* [bxc #27850](https://bugzilla.xamarin.com/show_bug.cgi?id=27850): Убедитесь, `using System.Runtime.InteropServices;` формируется, когда `DllImport` используется.
* [bxc #27852](https://bugzilla.xamarin.com/show_bug.cgi?id=27852): по умолчанию `DllImport` загрузку символов с `__Internal`.
* [bxc #27848](https://bugzilla.xamarin.com/show_bug.cgi?id=27848): пропустить объявлен прямого объявления Objective-C контейнера.
* [bxc #27846](https://bugzilla.xamarin.com/show_bug.cgi?id=27846): привязки протокола типов с помощью одного уточнения как конкретная интерфейсы (`id<Foo>` как `Foo` вместо `Foundation.NSObject<Foo>`).
* [bxc #28037](https://bugzilla.xamarin.com/show_bug.cgi?id=28037): привязка `UInt32`, `UInt64`, и `Int64` литералы в качестве `Int32` удаление `u` и/или `uL` суффиксами при значения можно безопасно использовать в `Int32`.
* [bxc #28038](https://bugzilla.xamarin.com/show_bug.cgi?id=28038): исправить сопоставления имен enum, когда исходный собственное имя начинается с `k` префикс.
* `sizeof` Выражения в C, тип аргумента не соответствует примитивный тип C# оценивается в Clang и привязаны как целочисленный литерал, чтобы избежать возникновения недопустимый C#.
* Исправьте синтаксис Objective-C для свойства, тип которого является блоком (в комментарии выше связанных объявлений отображается код Objective-C).
* Привязки типов decayed как исходным типом (`int[]` decays для `int*` во время семантического анализа Clang, но привязать его как исходный как записано `int[]` вместо).

Спасибо, что в значительной степени для Dave Dunkin reporting многие ошибки, исправленные в этом выпуске точка!

## <a name="200-march-9-2015"></a>2.0.0: 9 марта 2015 г.

[Загрузить v2.0.0](https://download.xamarin.com/objective-sharpie/ObjectiveSharpie-2.0.0.pkg)

Цели Sharpie 2.0 является основном выпуске, улучшенная драйвера, на основе Clang и средство синтаксического анализа и механизм привязки на основе NRefactory. Эти улучшенные компоненты обеспечения _большая_ более высокий уровень привязки, особенно вокруг тип привязки. Были внесены несколько улучшений внутренних Sharpie цель, которое создаст множество пользовательских возможностей в будущих выпусках.

Цели Sharpie 2.0 основана на Clang 3.6.1.

### <a name="type-binding-improvements"></a>Усовершенствования типа привязки

* Блоки Objective-C теперь поддерживаются. Это включает в себя анонимных или встроенных блоков и блоков с именем через `typedef`. Анонимные блоки будут привязаны как `System.Action` или `System.Func` делегаты, а именованные блоки будут действовать как строго именованной `delegate` типов.

* Нет улучшенные эвристики именования для анонимного перечисления, перед которым идет `typedef` разрешения в целочисленный тип builtin, таких как `long` или `int`.

* Указатели C сейчас привязаны как в C# `unsafe` указатели вместо `System.IntPtr`. Это приведет к более ясности в привязке для Если требуется включить параметров-указателей в `out` или `ref` параметров. Невозможно вывести, всегда ли параметр `out` или `ref`, поэтому курсор сохраняется в привязке, чтобы разрешить для упрощения аудита.

* В приведенном выше привязку указатель исключение при обнаружении ранг 2 указатель на объект Objective-C в качестве параметра. В этих случаях соглашение преобладающим и параметр будет привязан как `out` (например `NSError **error` → `out NSError error`).

### <a name="verify-attribute"></a>Проверьте атрибут

Вы обнаружите, что созданные Sharpie цели привязки теперь будет сопровождаться атрибутом `[Verify]` атрибута. Эти атрибуты указывают, что следует _проверить_ , Sharpie цель выполнялось правильнее сравнение привязка, с помощью исходного объявления C, Objective-C (который будет предоставляться в комментарии перед объявлением связанных).

Проверка будет _рекомендуется_ для всех связанных объявлений, но вероятнее всего _необходимые_ для объявления, сопровождаемых `[Verify]` атрибута. Это так, как во многих ситуациях, не хватает метаданных в исходного машинного кода, чтобы определить, как лучше всего создать привязку. Необходимо ссылаться документации или комментарии к коду в файлах заголовка для принятия наиболее привязки.

В разделе [Проверьте атрибуты](~/cross-platform/macios/binding/objective-sharpie/platform/verify.md) документации для получения дополнительных сведений.

### <a name="other-notable-improvements"></a>Другие важные улучшения

* `using` операторы теперь создаются на основе типов привязан. Например если `NSURL` был привязан `using Foundation;` также создание инструкций.

* `struct` и `union` объявления привязывается, с помощью `[FieldOffset]` перебор для объединения.

* Значения перечисления с инициализаторами константное выражение правильно привязывается; Полное выражение преобразуется в C#.

* Методы с переменным числом аргументов и блоки теперь привязан.

* Платформы теперь поддерживаются через `-framework` параметр. См. в документации на [привязки встроенных инфраструктур](http://developer.xamarin.com/guides/ios/advanced_topics/binding_objective-c/objective_sharpie/#frameworks) для получения дополнительных сведений.

* Objective-C исходного кода будет автоматически, которого следует устранить необходимость в передаче `-ObjC` или `-xobjective-c` для Clang вручную.

* Использование модуля clang (`@import`) определяется теперь автоматически, которого следует устранить необходимость в передаче `-fmodules` для Clang вручную для библиотек, которые используют новый модуль поддержки в Clang.

* API единой Xamarin теперь является целевым объектом привязки по умолчанию; использовать `-classic` параметр ориентироваться только классический API 32-разрядной.

### <a name="notable-bug-fixes"></a>Важные исправления ошибок

* Исправьте `instancetype` привязки при использовании в категорию Objective-c.
* Полное имя категории Objective-C
* Префикс протоколов Objective-C `I` (например `INSCopying` вместо `NSCopying`)

## <a name="1135-december-21-2014"></a>1.1.35: 21 декабря 2014 г.

[Загрузить v1.1.35](https://download.xamarin.com/objective-sharpie/ObjectiveSharpie-1.1.35.pkg)

Незначительных ошибок.

## <a name="111-december-15-2014"></a>1.1.1: 15 декабря 2014 г.

[Загрузка версии 1.1.1](https://download.xamarin.com/objective-sharpie/ObjectiveSharpie-1.1.1.pkg)

1.1.1 был первый основной версии после полтора года для внутреннего использования и разработки в Xamarin после начальной предварительной версии Sharpie цель на апрель 2013 г. Этот выпуск является первым обычно считалась стабильной и может использоваться для разнообразных собственных библиотек, содержащих новую серверную часть Clang.

